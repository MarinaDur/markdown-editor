import { styled } from "styled-components";

const CodeCodeBlock = styled.code`
  font-family: "Roboto Mono", sans-serif;
  font-size: 1.4rem;
  font-weight: 400;
  line-height: 24px;
  color: var(--cl-text-main);
`;

export default CodeCodeBlock;
