import { css } from "styled-components";

const textInputNameAndAll = css`
  color: var(--cl-white-global);
  font-family: "Roboto", sans-serif;
  font-size: 1.5rem;
  line-height: normal;
`;

export default textInputNameAndAll;
